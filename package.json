{
  "name": "game-search-engine",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently --names \"SERVER,CLIENT\" --prefix-colors \"magenta,cyan\" \"npm --workspace server run dev\" \"npm --workspace client run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
